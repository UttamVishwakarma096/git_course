const age = 24;
const name = "uttam";
const gender = "male";
const dob = "26/06/2002";

const greeting = () => {
  console.log(`Hello from ${name}`);
};

const details = () => {
  console.log(`name is ${name}\nage is ${age}\ngender is ${gender}`);
};

greeting();
